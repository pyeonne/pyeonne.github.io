---
import { ChevronUp } from 'lucide-react';
---

<a
  x-data="{ showScroll: false }"
  href="#"
  class="scrollup"
  :class="{ 'show-scroll': showScroll }"
  id="scroll-up"
  @scroll.window="showScroll = (window.scrollY >= 350 ? true : false)"
  ><ChevronUp /></a
>
